class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        

        # Set the precision of the decimal module to a suitable value
        

        # Input numerical strings
        

        # Perform the multiplication
        result = int(num1) * int(num2)

        # Convert the result back to a numerical string
        result_str = str(result)

        return result_str
